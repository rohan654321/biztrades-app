import CmsItemsPage from "../CmsItemsPage"

export default function BlogArticlesPage() {
  return (
    <CmsItemsPage
      type="BLOG"
      heading="Blog & Articles"
      description="Long-form articles with optional slug for SEO-friendly URLs."
    />
  )
}
